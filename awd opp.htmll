<html>
  <head><center>
    <style>
body {
  font-family: "Sofia";
  font-size: 19px;
}
</style>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load("current", {packages:["corechart"]});
      google.charts.setOnLoadCallback(drawChart);
      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          ['Task', 'Amount in $'],
          ['Half of Rent',     3000],
          ['Utilities, other bills', 300]
          ['Regular loan payment', 300],
          ['Laptop (Currently: 2010 tablet)',  999.00]
        ]);

        var options = {
          title: 'Budget Rough Draft',
          pieHole: 0.4,
        };

        var chart = new google.visualization.PieChart(document.getElementById('donutchart'));
        chart.draw(data, options);
      }
    </script>
  </head>
  <body>
    <div id="donutchart" style="width: 900px; height: 500px;"></div>
<p><br><br><br>

<img src ="/images/laptop.png" height="60%">
<p><br><br><br>

<b>Bonus: </b><br>
 + monitor to view lecture and work via dual monitor<br> 
 + headphones with mic<br>
 + $20 USB to USC-C Converter<br>

</html>
